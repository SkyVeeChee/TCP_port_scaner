TCP port scaner

Checks for all available TCP ports between two given numbers.
(numbers can be input in reverse.)
To run the programm input two numbers - numbers of the first and last port to be searched - and an IPA address,
all in a single line, separated by a whitespace.